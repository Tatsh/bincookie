{
  "configurations": [
    {
      "cwd": "${workspaceRoot}",
      "name": "Run tests",
      "program": "${workspaceRoot}/build/general_tests",
      "request": "launch",
      "type": "cppdbg"
    }
  ],
  "version": "0.2.0"
}
